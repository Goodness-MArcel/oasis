<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Welcome to Integrated Oasis</title>
    <style>
      body { font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; color:#111827; background:#f8fafc; margin:0; padding:0; }
      .container { max-width:600px; margin:24px auto; background:#ffffff; border-radius:12px; overflow:hidden; box-shadow:0 12px 34px rgba(15,23,42,0.06); }
      .header { padding:18px 20px; background: #ffffff; display:flex; align-items:center; gap:12px; }
      .logo { height:36px; }
      .content { padding:20px; }
      .cta { display:inline-block; margin-top:18px; padding:10px 14px; background:#16a34a; color:#fff; border-radius:8px; text-decoration:none; }
      .muted { color:#6b7280; font-size:0.95rem; }
      .footer { padding:14px 20px; background:#f1f5f9; font-size:0.85rem; color:#6b7280; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <img class="logo" src="<%= appUrl ? (appUrl.replace(/\/$/, '')) : '' %>/images/logo.png" alt="Integrated Oasis" />
        <div>
          <strong>Integrated Oasis</strong>
        </div>
      </div>
      <div class="content">
        <h2>Welcome, <%= fullName || '' %>!</h2>
        <p class="muted">Thanks for signing up. Your account has been created and you can now access courses and learning resources.</p>

        <p>To get started, sign in to your account:</p>
        <a class="cta" href="<%= appUrl ? (appUrl.replace(/\/$/, '')) : '' %>/auth">Sign in to your account</a>

        <p style="margin-top:18px" class="muted">If you did not create this account, please ignore this email or contact support.</p>
      </div>
      <div class="footer">Integrated Oasis • <%= appUrl || '' %></div>
    </div>
  </body>
</html>
